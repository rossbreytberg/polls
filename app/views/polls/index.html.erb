<ol class="poll_index">
  <% @polls.each do |poll| %>
    <li>
      <%= link_to poll.question, poll %>
    </li>
  <% end %>
</ol>

<br />

<% if signed_in? %>
  <%= link_to 'New Poll', new_poll_path %>
<% end %>
