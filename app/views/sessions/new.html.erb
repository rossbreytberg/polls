<h1>Login</h1>

<%= form_for(:session, url: sessions_path) do |f| %>
    <%= f.text_field :name, placeholder: "User" %>
    <%= f.password_field :password, placeholder: "Password" %>
    <%= f.submit "Login" %>
  </div>
<% end %>